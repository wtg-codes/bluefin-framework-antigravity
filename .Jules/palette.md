## 2024-04-22 - Dashboard Empty State\n**Learning:** Implementing an empty state with colSpan exceeding the number of columns (4 vs 3) is safe gracefully handled by browsers, but is inaccurate. Added an empty state for 'No recent builds found' when no workflows are returned.\n**Action:** Use sed locally with small line scopes to verify the actual number of elements (headers) before using colSpan.
